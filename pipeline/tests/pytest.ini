[pytest]
addopts = -q --durations=10 -v
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: tests requiring real data downloads
    unit: fast unit tests
    integration: integration tests